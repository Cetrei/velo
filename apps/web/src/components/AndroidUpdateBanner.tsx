// Deprecated. Replaced by UpdateNotificationBanner.tsx, which unifies Desktop app,
// Android app, and Backend update notifications into a single non-blocking banner
// that routes into Settings > Updates instead of installing inline.
// Kept here only because the Filesystem MCP has no delete operation; the original
// implementation is preserved at _deprecaated/web-components/AndroidUpdateBanner.tsx.
export {};
