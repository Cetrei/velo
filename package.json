{
  "name": "velo",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@tauri-apps/cli": "^2.0.0"
  },
  "private": true,
  "scripts": {
    "dev:web": "bun --filter web dev",
    "dev:server": "bun --filter server dev",
    "dev:desktop": "tauri dev --config apps/desktop/tauri.conf.json",
    "dev": "bun run dev:server && bun run dev:web",

    "build:desktop:tauri": "tauri build --config apps/desktop/tauri.conf.json",

    "build:desktop": "bun run build:desktop:tauri && bun scripts/collect-desktop-artifact.mjs",
    "build:android": "bun scripts/run-gradle-task.ts assembleRelease && bun scripts/collect-android-artifact.mjs",
    "build:server": "bun --filter server compile",
    "build:core": "cargo build --release --manifest-path crates/vcam-driver/Cargo.toml && bun scripts/collect-core-artifact.mjs",
    "build:all": "bun run build:desktop && bun run build:android",

    "typecheck:shared": "cd packages/shared && bun run typecheck",
    "typecheck:server": "cd apps/server && bun run typecheck",
    "typecheck:web": "cd apps/web && bun run typecheck",
    "typecheck:all": "bun run typecheck:shared && bun run typecheck:server && bun run typecheck:web",
    "check:rust": "cargo check --manifest-path apps/desktop/Cargo.toml && cargo check --manifest-path crates/vcam-driver/Cargo.toml",

    "verify:app": "bun run typecheck:all && bun run check:rust",
    "verify:backend": "bun run typecheck:server",
    "verify:web": "bun run typecheck:web",
    "verify:core": "cargo check --manifest-path crates/vcam-driver/Cargo.toml",

    "sync:version": "bun scripts/sync-version.ts",
    "release:app": "bun run install:all && bun run verify:app && bun scripts/release.ts",
    "release:backend": "bun run install:server && bun run verify:backend && bun scripts/release.ts --backend",
    "release:web": "bun run install:web && bun run verify:web && bun scripts/release.ts --web",
    "release:core": "bun run verify:core && bun scripts/release.ts --core",

    "install:root": "bun install",
    "install:server": "cd apps/server && bun install",
    "install:web": "cd apps/web && bun install",
    "install:rust": "cargo fetch --manifest-path apps/desktop/Cargo.toml && cargo fetch --manifest-path crates/vcam-driver/Cargo.toml",
    "install:android": "cd apps/web && npx cap sync",
    "install:all": "bun run install:root && bun run install:server && bun run install:web && bun run install:rust && bun run install:android"
  },
  "workspaces": [
    "apps/web",
    "apps/server",
    "packages/*"
  ]
}
